


<% if user_signed_in? %>

  <%# if bid.user_id == current_user.id %>

<%= simple_form_for [@auction, @auction.bids.build], remote: true do |f| %>
<%= f.input :amount, label: "Bid Amount: " %>
<%= f.button :submit %>
<% end %>



<% end %>
